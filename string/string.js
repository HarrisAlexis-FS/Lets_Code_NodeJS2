const uppercase = (str) => {
    return str.toUpperCase();
}

//lowercase function to lowercase test
const lowercase = (str) => str.toLowerCase()



module.exports = {uppercase, lowercase} //brackets to export multiple functions